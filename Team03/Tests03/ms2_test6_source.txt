procedure proc {
    if = 1 + 1;

    if (a == b) then {
        read a;
    } else {
        read b;
    }

    while = 2 + 2;

    while (a != 2) {
        read a;
    }

    if (b < 1 + 2) then {
        read c;
    } else {
        read d;
    }

    while (10 > c) {
        read e;
    }

    while (x + 2 * 3 <= e) {
        read f;
    } 

    if (x + (y - 3) % 10 >= 100 / z * (w + v)) then {
        read g;
    } else {
        read e;
    }

    while (!(x + 2 * 3 <= e)) {
        read z;
    }
    while (!(!(10 > c))) {
        read z;
    }
    while ((a != 2) && (a == b)) {
        read z;
    }
    while ((x + (y - 3) % 10 >= 100 / z * (w + v)) || (x + 2 * 3 <= e)) {
        read z;
    }
    while (((a == b) && (x + 2 * 3 <= e)) && ((a != 2) && (x + (y - 3) % 10 >= 100 / z * (w + v)))) {
        read z;
    }
    while (((b < 1 + 2) || (x + 2 * 3 <= e)) || ((10 > c) || (x + (y - 3) % 10 >= 100 / z * (w + v)))) {
        read z;
    }

    while ((a !=
    b
    )   ||            (12345          !=    z)) {

    read z;
    }

    if(1==1)then{read z;}else{read z;}
}